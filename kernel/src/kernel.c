#include "kernel.h"

int main(){

	//create_kernel_logger();
	load_configuration();
	kernel_server_init();

	/*
	int nro_proceso = 0;

	pcb *proceso = malloc(sizeof(pcb));
	proceso->id = nro_proceso; 
	//proceso->tamanioProceso =
	proceso->instr = lista;
	//proceso->tablaDePaginas = 
	proceso->estimacion_rafaga = valores_generales->est_inicial; 
	nro_proceso ++;

*/
	


	return 0;
}
